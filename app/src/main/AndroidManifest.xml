<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xieyaoyan.myapplication">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CustomA"></activity>
        <activity android:name=".CustomB"></activity>
        <activity android:name=".CustomC"></activity>
        <activity android:name=".CustomD"></activity>
        <activity android:name=".CustomE"></activity>
    </application>

</manifest>